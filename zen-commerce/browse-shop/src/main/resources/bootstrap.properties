#The value of spring.application.name property should match with base filename in global-config git hub repository
spring.application.name=browse-shop
spring.cloud.config.uri=http://localhost:8888
management.security.enabled=false